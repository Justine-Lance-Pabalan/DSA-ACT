#include <iostream>;

using namespace std;

void findMax(int arraynum, int Array_size)
{
    int maxindex = Array_size - 1;0
    sort(numarray, numarray + Array_size);
    cout <<"Maximum element: " <<numarray[maxindex] <<endl;
}
int main()
{
    cout <<"Enter size of array";
    cin >> Array_size;

    n = Array_size;

    int arraynum[i];

    cout <<"Enter number of element";
    for (int i = 0; i < n: i++)
    {
        cout <<"Enter element"<< i + 1<< ": ";
        cin >> arraynum[i];
    }

    findMax(element, Array_size)
    return0
}
